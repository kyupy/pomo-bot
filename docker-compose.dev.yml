services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    volumes:
      - .:/app:cached
      - node_modules:/app/node_modules
    env_file:
      - .env
    command: sh -lc "npm ci && npm run dev"

volumes:
  node_modules:
